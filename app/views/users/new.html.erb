<h1>New user</h1>

<% form_for(@user) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :login %><br />
    <%= f.text_field :login %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>



          <% form_for :user,@user,:url => { :action => "create",:controller => "users" } do |f| %>
            <%= oauth_register_button :value => "Login with Twitter account" %> 
          <% end %>

<%= link_to 'Back', users_path %>
